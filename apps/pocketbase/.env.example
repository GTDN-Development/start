# PocketBase env example
# Use the same shape for both `dev` and `prod` environments.

PB_SUPERUSER_EMAIL=hello@gtdn.online
PB_SUPERUSER_PASSWORD=XYZ

# optional safety override for the Mailpit apply script: ALLOW_PB_SETTINGS_WRITE=production
